import * as types from "./mutation-type";
import { Auth, List, Task } from "../api";

export default {
  login: ({ commit }) => {},
  fetchList: ({ commit }) => {},
  addTask: ({ commit }) => {},
  updateTask: ({ commit }) => {},
  removeTask: ({ commit }) => {},
  logout: ({ commit }) => {}
};
